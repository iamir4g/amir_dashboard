import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily:
    'Vazirmatn, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji"',
});
